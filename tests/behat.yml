default:
  autoload:
    '': '%paths.base%/contexts'
  extensions:
    Behat\MinkExtension:
      base_url: 'http://localhost/'
      goutte: ~
  suites:
    public:
      paths: [ '%paths.base%/features/public' ]
      contexts:
       - PublicContext:
           base_url: 'http://localhost/'
